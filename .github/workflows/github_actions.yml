name: Deploy to ECS (st / prod, OIDC)

on:
  push:
    branches:
      - main      # 本番
      - staging   # ステージング
  workflow_dispatch:

env:
  AWS_REGION: ap-northeast-1

jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      id-token: write      # OIDC認証に必須
      contents: read

    steps:
      # ===========================================
      # 1️⃣ ソースチェックアウト
      # ===========================================
      - name: Checkout repository
        uses: actions/checkout@v4

      # ===========================================
      # 2️⃣ デプロイ環境を設定
      # ===========================================
      - name: Set environment (staging or prod)
        id: set-env
        run: |
          if [[ "${GITHUB_REF_NAME}" == "staging" ]]; then
            echo "ENV=stage" >> $GITHUB_ENV
            echo "AWS_ACCOUNT_ID=389759593235" >> $GITHUB_ENV
            echo "ECS_CLUSTER=stage-cluster" >> $GITHUB_ENV
            echo "ECS_SERVICE=stage-service" >> $GITHUB_ENV
            echo "ROLE_ARN=arn:aws:iam::389759593235:role/tastylog-dev-githubactions-role" >> $GITHUB_ENV
          else
            echo "ENV=prod" >> $GITHUB_ENV
            echo "AWS_ACCOUNT_ID=550733172573" >> $GITHUB_ENV
            echo "ECS_CLUSTER=prod-cluster" >> $GITHUB_ENV
            echo "ECS_SERVICE=prod-service" >> $GITHUB_ENV
            echo "ROLE_ARN=arn:aws:iam::550733172573:role/tastylog-dev-githubactions-role" >> $GITHUB_ENV
          fi

      # ===========================================
      # 3️⃣ AWS 認証（OIDC）
      # ===========================================
      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      # ===========================================
      # 4️⃣ ECR ログイン・イメージビルド & プッシュ
      # ===========================================
      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build, tag, and push image
        run: |
          IMAGE_TAG=$(date +%Y%m%d%H%M%S)
          ECR_REPO="${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/${ENV}-ecr"
          docker build -t ${ECR_REPO}:${IMAGE_TAG} .
          docker push ${ECR_REPO}:${IMAGE_TAG}
          echo "IMAGE_URI=${ECR_REPO}:${IMAGE_TAG}" >> $GITHUB_ENV

      # ===========================================
      # 5️⃣ タスク定義テンプレートを展開
      # ===========================================
      - name: Render ECS task definition
        run: |
          envsubst < ecs-task-template.json > ecs-task.json
          echo "Task definition rendered:"
          cat ecs-task.json

      # ===========================================
      # 6️⃣ タスク定義を登録
      # ===========================================
      - name: Register ECS task definition
        id: register-task
        run: |
          TASK_DEF_ARN=$(aws ecs register-task-definition \
            --cli-input-json file://ecs-task.json \
            --query "taskDefinition.taskDefinitionArn" \
            --output text)
          echo "TASK_DEF_ARN=${TASK_DEF_ARN}" >> $GITHUB_ENV
          echo "Registered Task Definition: ${TASK_DEF_ARN}"

      # ===========================================
      # 7️⃣ サービスを更新（デプロイ）
      # ===========================================
      - name: Update ECS service
        run: |
          aws ecs update-service \
            --cluster ${ECS_CLUSTER} \
            --service ${ECS_SERVICE} \
            --task-definition ${TASK_DEF_ARN} \
            --force-new-deployment

      # ===========================================
      # ✅ 結果確認
      # ===========================================
      - name: Describe ECS service
        run: |
          aws ecs describe-services --cluster ${ECS_CLUSTER} --services ${ECS_SERVICE}
